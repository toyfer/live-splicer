# Live Splicer

AAC のライブ音源から MC・インターバルを切り、**残した区間に曲名を付けて曲ごとのトラック**として書き出すブラウザ完結型ツールです。処理は ffmpeg.wasm で、音源はサーバへ送信されません。

## できること

- 波形をドラッグして範囲を選び、**曲名付きトラック**として追加
- 無音区間（MC の切れ目）を自動検出してカット候補に追加
- **曲ごとに分割（無劣化 `-c copy`）** し、タイトル・アルバム・トラック番号・カバーを書き込み
- zip でまとめてダウンロード
- 1 本に結合する出力も残してあり、必要なら切り替え可能
- アルバム画像と共通タグ（アーティスト・ジャンル・年 など）
- 編集内容を JSON で保存・再読込（localStorage 自動保存つき）

## 使い方

1. 音源を選ぶ（ffmpeg-core の取得と解析・波形生成が走ります）
2. 波形をドラッグ → 曲名を入れ →「トラックにする」
3. 一覧で曲名・区間を確認（試聴・削除もここ）
4. 出力は既定の **曲ごとに分割（ライブラリ向け・無劣化）** のまま
5. アルバム名などを入れて「書き出す」→ 個別リンクまたは zip を取得

ミュージック / iTunes では、ファイルごとに違う曲名を入れ、**アルバム名を全曲で同じ**にするのがコツです。トラック番号は 1/総数 で自動付与されます。

## 出力モード

| | 曲ごとに分割（既定） | 1 本結合・無劣化 | 1 本結合・再エンコード |
|---|---|---|---|
| 中身 | 区間ごとに別 M4A、タグ付き | 残した区間だけ連結 | AAC を 1 回再エンコードして連結 |
| 用途 | ライブラリに曲として並べる | 1 本で最後まで聴く | 結合部の隙間を消したいとき |
| 音質 | 元と同一 | 元と同一 | 再エンコード分の変化 |
| カット境界 | AAC フレーム境界 | 同左 | サンプル単位 |

## GitHub Pages

`ffmpeg-core.wasm` はリポジトリに置きません。main への push で Actions が取得し、`lib/fflate.mjs` と合わせて公開物へ入れます。

初回だけ **Settings → Pages → Source: GitHub Actions** を選んでください。公開先は `https://toyfer.github.io/live-splicer/` です。シングルスレッド core なので COOP/COEP は不要です。

## 構成

```
live-splicer/
├── index.html
├── app.js
├── style.css
├── lib/                         zip 用 fflate（デプロイ時に取得）
└── .github/workflows/pages.yml
```

## 実装メモ

- 波形は `-ac 1 -ar 3000 -c:a pcm_s16le` の WAV を一度だけ作り、ピークを 2200 バケットで canvas 描画
- 分割は区間ごとに `-ss` / `-t` / `-c:a copy` / `-avoid_negative_ts make_zero` とメタデータを同時に指定
- 無音検出は波形ピークをしきい値で走査
- zip は `fflate` の `zipSync`

## 制約

- 出力はブラウザのメモリ上で組み立てます。極端に長い音源では失敗することがあります
- 「中断」のあとは「ffmpeg を読み込む」で復帰してください
- 分割・結合とも AAC フレーム境界でカットします（曲間の厳密さよりファイル分割が目的の用途向け）
